#create a function to:
    # 1)loop thru a string parameter
    # 2) print to console no. of vowels are in the argument 

#create a list for all known vowels
vowels = ["a", "e", "i", "o", "u"]

  count = 0

#function to count no. of vowels in user's name
def vowel_counting():
    for char in full_name.lower():
        if char in vowels:
            count += 1


    
     
